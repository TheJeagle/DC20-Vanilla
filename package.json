{
  "name": "dc20-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "node build.mjs && firebase deploy --only hosting,firestore:rules",
    "test": "node tests/stats.test.mjs && node tests/conformance.test.mjs && node tests/features.test.mjs",
    "evaluate:creatures": "node scripts/evaluateCreatures.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "claude": "^0.1.1"
  }
}
