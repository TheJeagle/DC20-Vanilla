{
  "name": "dc20-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "node build.mjs && firebase deploy --only hosting,firestore:rules",
    "test": "node tests/features.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
